node_modules/
.vercel/
.env
.env.*
dist/

# Scratch emitted by scripts/check-hosted-isolation.mjs (compiled api/ tree it
# traces with @vercel/nft). Always removed by the script; ignored so an
# interrupted run cannot dirty the tree.
.guard-dist/
.guard-tsconfig.json
